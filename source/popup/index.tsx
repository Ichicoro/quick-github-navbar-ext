import { h, render } from 'preact'
import Popup from './Popup'

render(<Popup />, document.body)
// render(<div className="bg-white" style={{ minWidth: 375, width: "maxContent", maxHeight: 450, padding: ".5rem" }}>
// 	<PopoverContents />
// </div>, document.body)
